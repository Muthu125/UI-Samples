<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="uiexamples.msf.com.uiandroidexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".uiactivities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".uiactivities.SlidingDrawer" />
        <activity android:name=".uiactivities.SpinnerAdapterUI" />
        <activity android:name=".uiactivities.DecimalTextView" />
        <activity android:name=".uiactivities.QuickSearchAction"/>
    </application>

</manifest>